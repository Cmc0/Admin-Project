export default {
    WEB_SOCKET_ID: 'WEB_SOCKET_ID',
    LOAD_MENU_FLAG: 'LOAD_MENU_FLAG', // 是否加载过菜单，作用：在 webSocket连接成功时，会判断是否加载过菜单，如果没有，则会再次加载菜单
    ADMIN_REDIRECT_PATH: 'ADMIN_REDIRECT_PATH', // 页面打开时，进行路由跳转的地址，为空则不跳转
}
