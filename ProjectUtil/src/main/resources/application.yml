server:
  port: 9526
spring:
  application:
    name: projectutil
  datasource:
    # 防止启动报错，下面可以随意配置
    url: jdbc:mysql://127.0.0.1:3306/not_set_this?characterEncoding=utf8&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=GMT%2B8
